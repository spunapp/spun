# Convex
CONVEX_DEPLOYMENT=your-convex-deployment
NEXT_PUBLIC_CONVEX_URL=https://your-deployment.convex.cloud

# App URL (used for Stripe redirect URLs)
NEXT_PUBLIC_APP_URL=https://spun.bot

# Stripe
STRIPE_SECRET_KEY=sk_live_...
STRIPE_WEBHOOK_SECRET=whsec_...

# OpenRouter (AI model access)
OPENROUTER_API_KEY=your-openrouter-api-key

# Google AI (Imagen 4 image generation for ad creatives)
GOOGLE_AI_API_KEY=your-google-ai-api-key

# Google Places API (New) — powers the Google Business Profile audit tool
# Enable "Places API (New)" in Google Cloud Console and restrict the key to that API.
GOOGLE_PLACES_API_KEY=your-google-places-api-key

# Clerk (auth)
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_...
CLERK_SECRET_KEY=sk_test_...
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/login
NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL=/chat
NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL=/chat
# Set this in Convex dashboard: npx convex env set CLERK_JWT_ISSUER_DOMAIN https://your-domain.clerk.accounts.dev
CLERK_JWT_ISSUER_DOMAIN=https://your-domain.clerk.accounts.dev

# Pipedream Connect — handles OAuth for Meta, Google, Klaviyo, TikTok (no per-platform developer apps needed)
# Get these from pipedream.com → your project → Connect → OAuth clients
PIPEDREAM_PROJECT_ID=proj_xxx
PIPEDREAM_CLIENT_ID=oa_xxx
PIPEDREAM_CLIENT_SECRET=your-pipedream-client-secret
# Optional: defaults to "production". Set to "development" if using a Pipedream development environment.
# PIPEDREAM_PROJECT_ENVIRONMENT=production
# Optional: extra origins allowed to embed the Connect iframe (JSON array or comma-separated)
# The request origin is always included automatically.
# PIPEDREAM_ALLOWED_ORIGINS=["http://localhost:3000"]
